// Root build file. The Android Gradle Plugin version is pinned so the build is
// reproducible; raise it together with the Gradle wrapper version.
plugins {
    id 'com.android.application' version '8.5.2' apply false
}
